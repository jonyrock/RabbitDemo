RabbitDemo
==========

OpenGL lightning demo. Enjoy.

##Install Packages
sudo apt-get install freeglut3 freeglut3-dev libglew-dev libglm-dev

##Build & Run
```
mkdir build 
cd build 
cmake ..
make
./rabbitDemo
```
